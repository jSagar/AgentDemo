# Skills

A **Skill** is a specific capability/function the agent can call (like a tool).

In this demo:
- `summarizeSkill.js` creates a compact summary of tasks.
- `prioritizeSkill.js` sorts tasks by priority (high → medium → low).

Skills are intentionally narrow and reusable.
