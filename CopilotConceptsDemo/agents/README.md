# Agents

An **Agent** is an autonomous AI that can plan, reason, and take multi-step actions.

In this demo, `taskAgent.js` shows a simple agent lifecycle:
1. Accept a high-level goal.
2. Build a plan.
3. Call skills.
4. Produce a final user-facing result.

The agent is the orchestrator — it does not replace skills, it coordinates them.
